1-)String input from the command line.
2-)5 word buffer management.(can be increased according to preference)
3-)When the new word is read :
	- if the new word is not in the buffer, 
	the oldest string in the buffer is discarded from the buffer and replaced with the newest.
	- if new word exists in the buffer,This string will be marked as newest.